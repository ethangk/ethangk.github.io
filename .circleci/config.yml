version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build:
    executor:
      name: node/default
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: |
                git config user.email "ethangk@gmail.com"
                git config user.name "Ethan Katzenberg"
            - add_ssh_keys:
                fingerprints:
                  - "53:b8:51:16:4e:2d:59:d6:8f:18:e5:03:ac:ac:15:cb"
            - run: npm run predeploy
            - run: npm run deploy
workflows:
    build:
      jobs:
        - build:
            filters:
              branches:
                only: content
